<div>
  <h2><%= @title %></h2>

  <.form
    :let={f}
    for={@changeset}
    id="driver-form"
    phx-target={@myself}
    phx-change="validate"
    phx-submit="save"
  >
    <%= label(f, :name) %>
    <%= text_input(f, :name) %>
    <%= error_tag(f, :name) %>

    <%= label(f, :cpf) %>
    <%= text_input(f, :cpf) %>
    <%= error_tag(f, :cpf) %>

    <%= label(f, :cnh) %>
    <%= text_input(f, :cnh) %>
    <%= error_tag(f, :cnh) %>

    <div>
      <%= submit("Save", phx_disable_with: "Saving...") %>
    </div>
  </.form>
</div>
